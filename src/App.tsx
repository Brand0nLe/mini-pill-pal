import React from 'react';
import AlertManager from './components/Alert/AlertManager';

const App: React.FC = () => {
  return (
    <div>
      {/* Other components */}
      <AlertManager />
    </div>
  );
};

export default App;
